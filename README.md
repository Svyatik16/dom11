# dom11
УСЛОВИЯ ЗАДАНИЯ:
С одномерным массивом, состоящим из n вводимых с клавиатуры положительных и отрицательных целых чисел выполнить следующие вычисления: сумму всех элементов массива, расположенных до последнего положительного элемента.

АЛГОРИТМ И БЛОК-СХЕМА:
1. Начало.
   
2. Ввод размера массива n

3. Проверка корректности n

4. Объявление массива arr[MAX_SIZE]

5. Ввод элементов массива

6. Поиск последнего положительного элемента

7. Вычисление суммы элементов до него

8. Вывод результата
  
9. Конец

БЛОК-СХЕМА:

<img width="463" height="906" alt="image" src="https://github.com/user-attachments/assets/45987dc2-b769-4e32-bcd3-fd5f88b7faec" />


РЕАЛИЗАЦИЯ ПРОГРАММЫ:

#include <stdio.h>
#include <locale.h>
#define MAX_SIZE 100
int main() {
    setlocale(LC_ALL, "RUS");
    int n;

    printf("Введите размер массива: ");
    scanf("%d", &n);

    int arr[MAX_SIZE];

    printf("Введите %d элементов массива:\n", n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    int last_plus_index = -1;
    for (int i = 0; i < n; i++) {
        if (arr[i] > 0) {
            last_plus_index = i;
        }
    }

    int sum = 0;
    if (last_plus_index != -1) {
        for (int i = 0; i < last_plus_index; i++) {
            sum += arr[i];
        }
        printf("Сумма элементов до последнего положительного: %d\n", sum);
    }
    else {
        printf("В массиве нет положительных элементов\n");
    }

    return 0;
}

РЕЗУЛЬТАТ РАБОТЫ ПРОГРАММЫ:

Введите размер массива: 3
Введите 3 элементов массива:
-1
2
3
Сумма элементов до последнего положительного: 1


ИНФОРМАЦИЯ О РАЗРАБОТЧИКЕ: бТТИ-251 Артемушкин Святослав




